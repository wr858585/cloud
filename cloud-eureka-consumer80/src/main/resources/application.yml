server:
  port: 80
spring:
  application:
    name: cloud-eureka-consumer80
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    server-url:
      defaultZone: http://localhost:7001/eureka